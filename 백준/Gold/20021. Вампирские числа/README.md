# [Gold III] Вампирские числа - 20021

[문제 링크](https://www.acmicpc.net/problem/20021)

### 성능 요약

시간 제한: 2 초, 메모리 제한: 512 MB

### 통계

제출: 13, 정답: 7, 맞힌 사람: 5, 정답 비율: 55.556%

### 분류

수학, 문자열, 브루트포스 알고리즘, 애드 혹, 정수론, 해 구성하기, 런타임 전의 전처리, 소인수분해

### 문제 설명

<p>Илья увлекается математикой. Недавно он прочитал про <em>вампирские числа</em>. Они настолько восхитили Илью, что теперь он постоянно придумывает задачи, связанные с этими числами, и пытается их решить.</p>

<p>Число $a$, десятичная запись которого состоит из $n$ цифр ($n$ четно), называется вампирским, если его можно представить в виде произведения двух $n/2$-значных чисел $b$ и $c$, причем используя все цифры $b$ и $c$ можно записать число $a$. Каждую цифру при этом разрешается использовать столько раз, сколько раз она суммарно встречается в $b$ и в $c$. Числа $b$ и $c$ называются <em>клыками</em> числа $a$.</p>

<p>Например, &nbsp;число $6880$ --- вампирское, так как $6880 = 80 \times 86$, а число $1023$ --- нет.</p>

<p>Для его новой задачи Илья попросил вас найти $k$ различных вампирских чисел, состоящих из $n$ цифр.</p>

### 입력

<p>В единственной строке входного даны два числа $k$ и $n$ --- требуемое количество вампирских чисел и количество цифр в каждом из них соответственно ($1 \le k \le 100$, $4 \le n \le 100$, $n$ --- четно).</p>

### 출력

<p>В выходной файл выведите $k$ различных $n$-значных вампирских числа в формате $A_i$=$B_{i}$x$C_{i}$, где $A_i$ --- $i$-е из найденных вампирских чисел, $B_i$ и $C_i$~--- его клыки&nbsp; (между $B_i$ и $C_i$ следует вывести маленькую латинскую букву &lt;&lt;<code>x</code>&gt;&gt;).&nbsp;</p>

<p>Если ответов несколько, то разрешается вывести любой из них. Гарантируется, что для приведенных во входном файле $n$ и $k$ существует $k$ различных $n$-значных вампирских чисел.</p>